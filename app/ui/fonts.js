import { Viga, Oxygen } from 'next/font/google';

export const viga = Viga({
  weight: ['400'],
  subsets: ['latin'],
  display: 'swap',
});

export const oxygen = Oxygen({
  weight: ['400', '700'],
  subsets: ['latin'],
  display: 'swap',
});
